import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wjkba.booxhighlights',
  appName: 'boox-highlights',
  webDir: 'dist'
};

export default config;
